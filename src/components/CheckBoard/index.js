import React from "react";
import { BoardContainer, CBoard } from "./CheckBoard";

const CheckBoard = () => {
  return (
    <BoardContainer>
      <CBoard></CBoard>
    </BoardContainer>
  );
};

export default CheckBoard;
